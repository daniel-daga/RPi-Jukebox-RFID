[pytest]
# Default collection for the generic python test run (pythonpackage.yml).
# The Spotify feature tests under tests/spotify need extra system
# dependencies (mopidy, mopidy-mpd, php) and run in their own workflow
# via tests/spotify/run_tests.sh, which passes the path explicitly.
testpaths = components
